{
  "name": "bsnl-sim-api",
  "version": "1.0.0",
  "private": true,
  "description": "BSNL SIM Portal Node.js API (MongoDB Atlas + Render)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.js",
    "deploy": "node copy-web.js && wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.16.0"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
